{
  "name": "missao-africa",
  "version": "1.0.0",
  "description": "Missao Africa",
  "author": "Daniel Rodrigues",
  "license": "ISC",
  "engines": {
    "yarn": "1.7.0"
  },
  "scripts": {
    "start": "NODE_ENV=production npm run start-server --prefix server",
    "postinstall": "NPM_CONFIG_PRODUCTION=false YARN_PRODUCTION=false npm run install-server --prefix server && npm run install-client --prefix client",
    "heroku-postbuild": "npm run build-client --prefix client"
  },
  "dependencies": {
    "concurrently": "^3.6.0"
  }
}