{
  "name": "missao-africa",
  "version": "1.0.0",
  "description": "Missao Africa",
  "author": "Daniel Rodrigues",
  "license": "ISC",
  "engines": {
    "yarn": "1.7.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "NODE_ENV=production npm run start --prefix server",
    "postinstall": "npm run install:server --prefix server && npm run install:client --prefix client",
    "heroku-postbuild": "npm run build --prefix client"
  },
  "dependencies": {
    "concurrently": "^3.6.0"
  }
}